spring:
  batch:
    jdbc:
      initialize-schema: always
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 111111
    url: jdbc:mysql://localhost:3306/web_magazine?&serverTimezone=UTC&autoReconnect=true&allowMultiQueries=true&characterEncoding=UTF-8
    mapper-locations: classpath:com/aza/mapper/*.xml
  devtools:
    livereload:
      enabled: true
    restart:
      log-condition-evaluation-delta: false
logging:
  level:
    org.springframework.boot: ERROR